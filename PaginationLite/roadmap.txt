Lets hide/grey out the print via browser optoin for now since that's not working great and we shoold rely on the pdf export instead....
automatically create extra page if odd page count?
call current 'Kindle Standard A' 'Kindle Paperback - 5 x 8' instead and make this the default (rather than Custom option)

Ensure rendered images have same scale/size as in Word

Do research to refine margin amounts specifically for kindle paperback based on net feedback and kindle documentation